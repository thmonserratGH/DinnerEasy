import { BorderErrorDirective } from './border-error.directive';

describe('BorderErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderErrorDirective();
    expect(directive).toBeTruthy();
  });
});
